version: "2"
services:
  db:
    image: "postgres:latest"
    ports:
     - "5432:5432"
    environment:
      POSTGRES_USER: "newslab"
      POSTGRES_PASSWORD: "balswen"
      POSTGRES_DB: "newslab-dev"